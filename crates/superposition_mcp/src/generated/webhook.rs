// AUTO-GENERATED by smithy mcp-codegen — DO NOT EDIT
use rmcp::model::*;
use schemars::JsonSchema;
use serde::Deserialize;

use crate::SuperpositionMcpServer;
use crate::helpers::*;

/// Retrieves detailed information about a specific webhook config, including its events, headers, and trigger history.
#[derive(Debug, Deserialize, JsonSchema)]
pub struct GetWebhookParams {
    pub name: String,
}

/// Updates an existing webhook config, allowing modification of URL, events, headers, and other webhook properties.
#[derive(Debug, Deserialize, JsonSchema)]
pub struct UpdateWebhookParams {
    pub name: String,
    pub description: Option<String>,
    pub enabled: Option<bool>,
    pub url: Option<String>,
    pub method: Option<String>,
    pub version: Option<String>,
    pub custom_headers: Option<serde_json::Value>,
    pub events: Option<Vec<String>>,
    pub change_reason: String,
}

/// Permanently removes a webhook config from the workspace, stopping all future event notifications to that endpoint.
#[derive(Debug, Deserialize, JsonSchema)]
pub struct DeleteWebhookParams {
    pub name: String,
}

/// Retrieves a paginated list of all webhook configs in the workspace, including their status and config details.
#[derive(Debug, Deserialize, JsonSchema)]
pub struct ListWebhookParams {
    /// Number of items to be returned in each page.
    pub count: Option<i32>,
    /// Page number to retrieve, starting from 1.
    pub page: Option<i32>,
    /// If true, returns all requested items, ignoring pagination parameters page and count.
    pub all: Option<bool>,
}

/// Creates a new webhook config to receive HTTP notifications when specified events occur in the system.
#[derive(Debug, Deserialize, JsonSchema)]
pub struct CreateWebhookParams {
    pub name: String,
    pub description: String,
    pub enabled: bool,
    pub url: String,
    pub method: String,
    pub version: Option<String>,
    pub custom_headers: Option<serde_json::Value>,
    pub events: Vec<String>,
    pub change_reason: String,
}

/// Retrieves a webhook configuration based on a specific event type, allowing users to find which webhook is set to trigger for that event.
#[derive(Debug, Deserialize, JsonSchema)]
pub struct GetWebhookByEventWebhookParams {
    pub event: String,
}

impl SuperpositionMcpServer {
    pub async fn get_webhook_impl(&self, args: GetWebhookParams) -> Result<CallToolResult, rmcp::ErrorData> {
        let mut req = self.client.get_webhook()
            .workspace_id(&self.config.workspace_id)
            .org_id(&self.config.org_id);
        req = req.name(args.name);
        let resp = req.send().await.map_err(mcp_err)?;
        let json = serde_json::to_string_pretty(&webhook_to_json!(resp)).map_err(mcp_err)?;
        Ok(CallToolResult::success(vec![Content::text(json)]))
    }

    pub async fn update_webhook_impl(&self, args: UpdateWebhookParams) -> Result<CallToolResult, rmcp::ErrorData> {
        let mut req = self.client.update_webhook()
            .workspace_id(&self.config.workspace_id)
            .org_id(&self.config.org_id);
        req = req.name(args.name);
        req = req.change_reason(args.change_reason);
        if let Some(v) = args.description {
            req = req.description(v);
        }
        if let Some(v) = args.enabled {
            req = req.enabled(v);
        }
        if let Some(v) = args.url {
            req = req.url(v);
        }
        if let Some(v) = args.method {
            req = req.method(v);
        }
        if let Some(v) = args.version {
            req = req.version(v);
        }
        if let Some(v) = args.custom_headers {
            req = req.set_custom_headers(Some(json_to_doc_map(v).map_err(mcp_err)?));
        }
        if let Some(v) = args.events {
            for item in v {
                req = req.events(item);
            }
        }
        let resp = req.send().await.map_err(mcp_err)?;
        let json = serde_json::to_string_pretty(&webhook_to_json!(resp)).map_err(mcp_err)?;
        Ok(CallToolResult::success(vec![Content::text(json)]))
    }

    pub async fn delete_webhook_impl(&self, args: DeleteWebhookParams) -> Result<CallToolResult, rmcp::ErrorData> {
        let mut req = self.client.delete_webhook()
            .workspace_id(&self.config.workspace_id)
            .org_id(&self.config.org_id);
        req = req.name(args.name);
        let resp = req.send().await.map_err(mcp_err)?;
        let json = "Deleted successfully".to_string();
        Ok(CallToolResult::success(vec![Content::text(json)]))
    }

    pub async fn list_webhook_impl(&self, args: ListWebhookParams) -> Result<CallToolResult, rmcp::ErrorData> {
        let mut req = self.client.list_webhook()
            .workspace_id(&self.config.workspace_id)
            .org_id(&self.config.org_id);
        if let Some(v) = args.count {
            req = req.count(v);
        }
        if let Some(v) = args.page {
            req = req.page(v);
        }
        if let Some(v) = args.all {
            req = req.all(v);
        }
        let resp = req.send().await.map_err(mcp_err)?;
        let items: Vec<serde_json::Value> = resp.data.iter().map(|r| webhook_to_json!(r)).collect();
        let result = serde_json::json!({
            "total_pages": resp.total_pages,
            "total_items": resp.total_items,
            "data": items,
        });
        let json = serde_json::to_string_pretty(&result).map_err(mcp_err)?;
        Ok(CallToolResult::success(vec![Content::text(json)]))
    }

    pub async fn create_webhook_impl(&self, args: CreateWebhookParams) -> Result<CallToolResult, rmcp::ErrorData> {
        let mut req = self.client.create_webhook()
            .workspace_id(&self.config.workspace_id)
            .org_id(&self.config.org_id);
        req = req.name(args.name);
        req = req.description(args.description);
        req = req.enabled(args.enabled);
        req = req.url(args.url);
        req = req.method(args.method);
        for item in args.events {
            req = req.events(item);
        }
        req = req.change_reason(args.change_reason);
        if let Some(v) = args.version {
            req = req.version(v);
        }
        if let Some(v) = args.custom_headers {
            req = req.set_custom_headers(Some(json_to_doc_map(v).map_err(mcp_err)?));
        }
        let resp = req.send().await.map_err(mcp_err)?;
        let json = serde_json::to_string_pretty(&webhook_to_json!(resp)).map_err(mcp_err)?;
        Ok(CallToolResult::success(vec![Content::text(json)]))
    }

    pub async fn get_webhook_by_event_webhook_impl(&self, args: GetWebhookByEventWebhookParams) -> Result<CallToolResult, rmcp::ErrorData> {
        let mut req = self.client.get_webhook_by_event()
            .workspace_id(&self.config.workspace_id)
            .org_id(&self.config.org_id);
        req = req.event(args.event);
        let resp = req.send().await.map_err(mcp_err)?;
        let json = serde_json::to_string_pretty(&webhook_to_json!(resp)).map_err(mcp_err)?;
        Ok(CallToolResult::success(vec![Content::text(json)]))
    }

}
