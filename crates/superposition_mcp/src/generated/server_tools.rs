// AUTO-GENERATED by smithy mcp-codegen — DO NOT EDIT
use rmcp::handler::server::wrapper::Parameters;
use rmcp::model::*;
use rmcp::{tool, tool_router};

use crate::SuperpositionMcpServer;
use crate::generated::tools::*;

#[tool_router]
impl SuperpositionMcpServer {
    // ===== DefaultConfig =====
    #[tool(
        name = "default_config.get",
        description = "Retrieves a specific default config entry by its key, including its value, schema, function mappings, and metadata."
    )]
    async fn default_config_get(
        &self,
        Parameters(args): Parameters<GetDefaultConfigParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.get_default_config_impl(args).await
    }

    #[tool(
        name = "default_config.update",
        description = "Updates an existing default config entry. Allows modification of value, schema, function mappings, and description while preserving the key identifier."
    )]
    async fn default_config_update(
        &self,
        Parameters(args): Parameters<UpdateDefaultConfigParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.update_default_config_impl(args).await
    }

    #[tool(
        name = "default_config.delete",
        description = "Permanently removes a default config entry from the workspace. This operation cannot be performed if it affects config resolution for contexts that rely on this fallback value."
    )]
    async fn default_config_delete(
        &self,
        Parameters(args): Parameters<DeleteDefaultConfigParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.delete_default_config_impl(args).await
    }

    #[tool(
        name = "default_config.list",
        description = "Retrieves a paginated list of all default config entries in the workspace, including their values, schemas, and metadata."
    )]
    async fn default_config_list(
        &self,
        Parameters(args): Parameters<ListDefaultConfigParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.list_default_config_impl(args).await
    }

    #[tool(
        name = "default_config.create",
        description = "Creates a new default config entry with specified key, value, schema, and metadata. Default configs serve as fallback values when no specific context matches."
    )]
    async fn default_config_create(
        &self,
        Parameters(args): Parameters<CreateDefaultConfigParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.create_default_config_impl(args).await
    }

    // ===== Dimension =====
    #[tool(
        name = "dimension.get",
        description = "Retrieves detailed information about a specific dimension, including its schema, cohort dependency graph, and configuration metadata."
    )]
    async fn dimension_get(
        &self,
        Parameters(args): Parameters<GetDimensionParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.get_dimension_impl(args).await
    }

    #[tool(
        name = "dimension.update",
        description = "Updates an existing dimension's configuration. Allows modification of schema, position, function mappings, and other properties while maintaining dependency relationships."
    )]
    async fn dimension_update(
        &self,
        Parameters(args): Parameters<UpdateDimensionParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.update_dimension_impl(args).await
    }

    #[tool(
        name = "dimension.delete",
        description = "Permanently removes a dimension from the workspace. This operation will fail if the dimension has active dependencies or is referenced by existing configurations."
    )]
    async fn dimension_delete(
        &self,
        Parameters(args): Parameters<DeleteDimensionParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.delete_dimension_impl(args).await
    }

    #[tool(
        name = "dimension.list",
        description = "Retrieves a paginated list of all dimensions in the workspace. Dimensions are returned with their details and metadata."
    )]
    async fn dimension_list(
        &self,
        Parameters(args): Parameters<ListDimensionParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.list_dimension_impl(args).await
    }

    #[tool(
        name = "dimension.create",
        description = "Creates a new dimension with the specified json schema. Dimensions define categorical attributes used for context-based config management."
    )]
    async fn dimension_create(
        &self,
        Parameters(args): Parameters<CreateDimensionParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.create_dimension_impl(args).await
    }

    // ===== Context =====
    #[tool(
        name = "context.create",
        description = "Creates a new context with specified conditions and overrides. Contexts define conditional rules for config management."
    )]
    async fn context_create(
        &self,
        Parameters(args): Parameters<CreateContextParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.create_context_impl(args).await
    }

    #[tool(
        name = "context.get",
        description = "Retrieves detailed information about a specific context by its unique identifier, including conditions, overrides, and metadata."
    )]
    async fn context_get(
        &self,
        Parameters(args): Parameters<GetContextParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.get_context_impl(args).await
    }

    #[tool(
        name = "context.delete",
        description = "Permanently removes a context from the workspace. This operation cannot be undone and will affect config resolution."
    )]
    async fn context_delete(
        &self,
        Parameters(args): Parameters<DeleteContextParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.delete_context_impl(args).await
    }

    #[tool(
        name = "context.list",
        description = "Retrieves a paginated list of contexts with support for filtering by creation date, modification date, weight, and other criteria."
    )]
    async fn context_list(
        &self,
        Parameters(args): Parameters<ListContextParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.list_context_impl(args).await
    }

    #[tool(
        name = "context.move",
        description = "Updates the condition of the mentioned context, if a context with the new condition already exists, it merges the override and effectively deleting the old context"
    )]
    async fn context_move(
        &self,
        Parameters(args): Parameters<MoveContextParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.move_context_impl(args).await
    }

    #[tool(
        name = "context.update_override",
        description = "Updates the overrides for an existing context. Allows modification of override values while maintaining the context's conditions."
    )]
    async fn context_update_override(
        &self,
        Parameters(args): Parameters<UpdateOverrideContextParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.update_override_context_impl(args).await
    }

    #[tool(
        name = "context.get_context_from_condition",
        description = "Retrieves context information by matching against provided conditions. Used to find contexts that would apply to specific scenarios."
    )]
    async fn context_get_context_from_condition(&self) -> Result<CallToolResult, rmcp::ErrorData> {
        self.get_context_from_condition_context_impl().await
    }

    #[tool(
        name = "context.weight_recompute",
        description = "Recalculates and updates the priority weights for all contexts in the workspace based on their dimensions."
    )]
    async fn context_weight_recompute(
        &self,
        Parameters(args): Parameters<WeightRecomputeContextParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.weight_recompute_context_impl(args).await
    }

    #[tool(
        name = "context.bulk_operation",
        description = "Executes multiple context operations (PUT, REPLACE, DELETE, MOVE) in a single atomic transaction for efficient batch processing."
    )]
    async fn context_bulk_operation(
        &self,
        Parameters(args): Parameters<BulkOperationContextParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.bulk_operation_context_impl(args).await
    }

    #[tool(
        name = "context.validate",
        description = "Validates if a given context condition is well-formed"
    )]
    async fn context_validate(
        &self,
        Parameters(args): Parameters<ValidateContextParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.validate_context_impl(args).await
    }

    // ===== Config =====
    #[tool(
        name = "config.get_config_fast",
        description = "Retrieves the latest config with no processing for high-performance access."
    )]
    async fn config_get_config_fast(&self) -> Result<CallToolResult, rmcp::ErrorData> {
        self.get_config_fast_config_impl().await
    }

    #[tool(
        name = "config.get",
        description = "Retrieves config data with context evaluation, including applicable contexts, overrides, and default values based on provided conditions."
    )]
    async fn config_get(
        &self,
        Parameters(args): Parameters<GetConfigParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.get_config_impl(args).await
    }

    #[tool(
        name = "config.get_resolved_config",
        description = "Resolves and merges config values based on context conditions, applying overrides and merge strategies to produce the final configuration."
    )]
    async fn config_get_resolved_config(
        &self,
        Parameters(args): Parameters<GetResolvedConfigConfigParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.get_resolved_config_config_impl(args).await
    }

    #[tool(
        name = "config.get_resolved_config_with_identifier",
        description = "Resolves and merges config values based on context conditions and identifier, applying overrides and merge strategies to produce the final configuration."
    )]
    async fn config_get_resolved_config_with_identifier(
        &self,
        Parameters(args): Parameters<GetResolvedConfigWithIdentifierConfigParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.get_resolved_config_with_identifier_config_impl(args).await
    }

    #[tool(
        name = "config.get_config_toml",
        description = "Retrieves the full config in TOML format, including default configs with schemas, dimensions, and overrides. This endpoint is optimized for clients that prefer TOML format for configuration managem..."
    )]
    async fn config_get_config_toml(&self) -> Result<CallToolResult, rmcp::ErrorData> {
        self.get_config_toml_config_impl().await
    }

    #[tool(
        name = "config.get_config_json",
        description = "Retrieves the full config in JSON format, including default configs with schemas, dimensions, and overrides. This endpoint is optimized for clients that prefer JSON format for configuration managem..."
    )]
    async fn config_get_config_json(&self) -> Result<CallToolResult, rmcp::ErrorData> {
        self.get_config_json_config_impl().await
    }

    // ===== ConfigVersion =====
    #[tool(
        name = "config_version.get",
        description = "Retrieves a specific config version along with its metadata for audit and rollback purposes."
    )]
    async fn config_version_get(
        &self,
        Parameters(args): Parameters<GetConfigVersionParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.get_config_version_impl(args).await
    }

    #[tool(
        name = "config_version.list",
        description = "Retrieves a paginated list of config versions with their metadata, hash values, and creation timestamps for audit and rollback purposes."
    )]
    async fn config_version_list(
        &self,
        Parameters(args): Parameters<ListConfigVersionParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.list_config_version_impl(args).await
    }

    // ===== AuditLog =====
    #[tool(
        name = "audit_log.list",
        description = "Retrieves a paginated list of audit logs with support for filtering by date range, table names, actions, and usernames for compliance and monitoring purposes."
    )]
    async fn audit_log_list(
        &self,
        Parameters(args): Parameters<ListAuditLogParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.list_audit_log_impl(args).await
    }

    // ===== Function =====
    #[tool(
        name = "function.get",
        description = "Retrieves detailed information about a specific function including its published and draft versions, code, and metadata."
    )]
    async fn function_get(
        &self,
        Parameters(args): Parameters<GetFunctionParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.get_function_impl(args).await
    }

    #[tool(
        name = "function.update",
        description = "Updates the draft version of an existing function with new code, runtime version, or description while preserving the published version."
    )]
    async fn function_update(
        &self,
        Parameters(args): Parameters<UpdateFunctionParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.update_function_impl(args).await
    }

    #[tool(
        name = "function.delete",
        description = "Permanently removes a function from the workspace, deleting both draft and published versions along with all associated code. It fails if already in use"
    )]
    async fn function_delete(
        &self,
        Parameters(args): Parameters<DeleteFunctionParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.delete_function_impl(args).await
    }

    #[tool(
        name = "function.list",
        description = "Retrieves a paginated list of all functions in the workspace with their basic information and current status."
    )]
    async fn function_list(
        &self,
        Parameters(args): Parameters<ListFunctionParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.list_function_impl(args).await
    }

    #[tool(
        name = "function.create",
        description = "Creates a new custom function for value_validation, value_compute, context_validation or change_reason_validation with specified code, runtime version, and function type."
    )]
    async fn function_create(
        &self,
        Parameters(args): Parameters<CreateFunctionParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.create_function_impl(args).await
    }

    #[tool(
        name = "function.test",
        description = "Executes a function in test mode with provided input parameters to validate its behavior before publishing or deployment."
    )]
    async fn function_test(
        &self,
        Parameters(args): Parameters<TestFunctionParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.test_function_impl(args).await
    }

    #[tool(
        name = "function.publish",
        description = "Publishes the draft version of a function, making it the active version used for value_validation, value_compute, context_validation or change_reason_validation in the system."
    )]
    async fn function_publish(
        &self,
        Parameters(args): Parameters<PublishFunctionParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.publish_function_impl(args).await
    }

    // ===== Organisation =====
    #[tool(
        name = "organisation.create",
        description = "Creates a new organisation with specified name and administrator email. This is the top-level entity that contains workspaces and manages organizational-level settings."
    )]
    async fn organisation_create(
        &self,
        Parameters(args): Parameters<CreateOrganisationParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.create_organisation_impl(args).await
    }

    #[tool(
        name = "organisation.get",
        description = "Retrieves detailed information about a specific organisation including its status, contact details, and administrative metadata."
    )]
    async fn organisation_get(
        &self,
        Parameters(args): Parameters<GetOrganisationParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.get_organisation_impl(args).await
    }

    #[tool(
        name = "organisation.update",
        description = "Updates an existing organisation's information including contact details, status, and administrative properties."
    )]
    async fn organisation_update(
        &self,
        Parameters(args): Parameters<UpdateOrganisationParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.update_organisation_impl(args).await
    }

    #[tool(
        name = "organisation.list",
        description = "Retrieves a paginated list of all organisations with their basic information, creation details, and current status."
    )]
    async fn organisation_list(
        &self,
        Parameters(args): Parameters<ListOrganisationParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.list_organisation_impl(args).await
    }

    // ===== Experiments =====
    #[tool(
        name = "experiments.create",
        description = "Creates a new experiment with variants, context and conditions. You can optionally specify metrics and experiment group for tracking and analysis."
    )]
    async fn experiments_create(
        &self,
        Parameters(args): Parameters<CreateExperimentsParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.create_experiments_impl(args).await
    }

    #[tool(
        name = "experiments.get",
        description = "Retrieves detailed information about a specific experiment, including its config, variants, status, and metrics."
    )]
    async fn experiments_get(
        &self,
        Parameters(args): Parameters<GetExperimentsParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.get_experiments_impl(args).await
    }

    #[tool(
        name = "experiments.list",
        description = "Retrieves a paginated list of experiments with support for filtering by status, date range, name, creator, and experiment group."
    )]
    async fn experiments_list(
        &self,
        Parameters(args): Parameters<ListExperimentsParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.list_experiments_impl(args).await
    }

    #[tool(
        name = "experiments.update_overrides_experiment",
        description = "Updates the overrides for specific variants within an experiment, allowing modification of experiment behavior Updates the overrides for specific variants within an experiment, allowing modificatio..."
    )]
    async fn experiments_update_overrides_experiment(
        &self,
        Parameters(args): Parameters<UpdateOverridesExperimentExperimentsParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.update_overrides_experiment_experiments_impl(args).await
    }

    #[tool(
        name = "experiments.conclude_experiment",
        description = "Concludes an inprogress experiment by selecting a winning variant and transitioning the experiment to a concluded state."
    )]
    async fn experiments_conclude_experiment(
        &self,
        Parameters(args): Parameters<ConcludeExperimentExperimentsParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.conclude_experiment_experiments_impl(args).await
    }

    #[tool(
        name = "experiments.discard_experiment",
        description = "Discards an experiment without selecting a winner, effectively canceling the experiment and removing its effects."
    )]
    async fn experiments_discard_experiment(
        &self,
        Parameters(args): Parameters<DiscardExperimentExperimentsParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.discard_experiment_experiments_impl(args).await
    }

    #[tool(
        name = "experiments.ramp_experiment",
        description = "Adjusts the traffic percentage allocation for an in-progress experiment, allowing gradual rollout or rollback of experimental features."
    )]
    async fn experiments_ramp_experiment(
        &self,
        Parameters(args): Parameters<RampExperimentExperimentsParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.ramp_experiment_experiments_impl(args).await
    }

    #[tool(
        name = "experiments.pause_experiment",
        description = "Temporarily pauses an inprogress experiment, suspending its effects while preserving the experiment config for later resumption."
    )]
    async fn experiments_pause_experiment(
        &self,
        Parameters(args): Parameters<PauseExperimentExperimentsParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.pause_experiment_experiments_impl(args).await
    }

    #[tool(
        name = "experiments.resume_experiment",
        description = "Resumes a previously paused experiment, restoring its in-progress state and re-enabling variant evaluation."
    )]
    async fn experiments_resume_experiment(
        &self,
        Parameters(args): Parameters<ResumeExperimentExperimentsParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.resume_experiment_experiments_impl(args).await
    }

    #[tool(
        name = "experiments.applicable_variants",
        description = "Determines which experiment variants are applicable to a given context, used for experiment evaluation and variant selection."
    )]
    async fn experiments_applicable_variants(
        &self,
        Parameters(args): Parameters<ApplicableVariantsExperimentsParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.applicable_variants_experiments_impl(args).await
    }

    // ===== TypeTemplates =====
    #[tool(
        name = "type_templates.get",
        description = "Retrieves detailed information about a specific type template including its schema and metadata."
    )]
    async fn type_templates_get(
        &self,
        Parameters(args): Parameters<GetTypeTemplatesParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.get_type_templates_impl(args).await
    }

    #[tool(
        name = "type_templates.update",
        description = "Updates an existing type template's schema definition and metadata while preserving its identifier and usage history."
    )]
    async fn type_templates_update(
        &self,
        Parameters(args): Parameters<UpdateTypeTemplatesParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.update_type_templates_impl(args).await
    }

    #[tool(
        name = "type_templates.delete",
        description = "Permanently removes a type template from the workspace. No checks performed while deleting"
    )]
    async fn type_templates_delete(
        &self,
        Parameters(args): Parameters<DeleteTypeTemplatesParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.delete_type_templates_impl(args).await
    }

    #[tool(
        name = "type_templates.list",
        description = "Retrieves a paginated list of all type templates in the workspace, including their schemas and metadata for type management."
    )]
    async fn type_templates_list(
        &self,
        Parameters(args): Parameters<ListTypeTemplatesParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.list_type_templates_impl(args).await
    }

    #[tool(
        name = "type_templates.create",
        description = "Creates a new type template with specified schema definition, providing reusable type definitions for config validation."
    )]
    async fn type_templates_create(
        &self,
        Parameters(args): Parameters<CreateTypeTemplatesParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.create_type_templates_impl(args).await
    }

    // ===== Workspace =====
    #[tool(
        name = "workspace.get",
        description = "Retrieves detailed information about a specific workspace including its configuration and metadata."
    )]
    async fn workspace_get(
        &self,
        Parameters(args): Parameters<GetWorkspaceParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.get_workspace_impl(args).await
    }

    #[tool(
        name = "workspace.update",
        description = "Updates an existing workspace configuration, allowing modification of admin settings, mandatory dimensions, and workspace properties. Validates config version existence if provided."
    )]
    async fn workspace_update(
        &self,
        Parameters(args): Parameters<UpdateWorkspaceParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.update_workspace_impl(args).await
    }

    #[tool(
        name = "workspace.list",
        description = "Retrieves a paginated list of all workspaces with optional filtering by workspace name, including their status, config details, and administrative information."
    )]
    async fn workspace_list(
        &self,
        Parameters(args): Parameters<ListWorkspaceParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.list_workspace_impl(args).await
    }

    #[tool(
        name = "workspace.create",
        description = "Creates a new workspace within an organisation, including database schema setup and isolated environment for config management with specified admin and settings."
    )]
    async fn workspace_create(
        &self,
        Parameters(args): Parameters<CreateWorkspaceParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.create_workspace_impl(args).await
    }

    #[tool(
        name = "workspace.migrate_workspace_schema",
        description = "Migrates the workspace database schema to the new version of the template"
    )]
    async fn workspace_migrate_workspace_schema(
        &self,
        Parameters(args): Parameters<MigrateWorkspaceSchemaWorkspaceParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.migrate_workspace_schema_workspace_impl(args).await
    }

    #[tool(
        name = "workspace.rotate_workspace_encryption_key",
        description = "Rotates the workspace encryption key. Generates a new encryption key and re-encrypts all secrets with the new key. This is a critical operation that should be done during low-traffic periods."
    )]
    async fn workspace_rotate_workspace_encryption_key(
        &self,
        Parameters(args): Parameters<RotateWorkspaceEncryptionKeyWorkspaceParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.rotate_workspace_encryption_key_workspace_impl(args).await
    }

    // ===== Webhook =====
    #[tool(
        name = "webhook.get",
        description = "Retrieves detailed information about a specific webhook config, including its events, headers, and trigger history."
    )]
    async fn webhook_get(
        &self,
        Parameters(args): Parameters<GetWebhookParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.get_webhook_impl(args).await
    }

    #[tool(
        name = "webhook.update",
        description = "Updates an existing webhook config, allowing modification of URL, events, headers, and other webhook properties."
    )]
    async fn webhook_update(
        &self,
        Parameters(args): Parameters<UpdateWebhookParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.update_webhook_impl(args).await
    }

    #[tool(
        name = "webhook.delete",
        description = "Permanently removes a webhook config from the workspace, stopping all future event notifications to that endpoint."
    )]
    async fn webhook_delete(
        &self,
        Parameters(args): Parameters<DeleteWebhookParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.delete_webhook_impl(args).await
    }

    #[tool(
        name = "webhook.list",
        description = "Retrieves a paginated list of all webhook configs in the workspace, including their status and config details."
    )]
    async fn webhook_list(
        &self,
        Parameters(args): Parameters<ListWebhookParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.list_webhook_impl(args).await
    }

    #[tool(
        name = "webhook.create",
        description = "Creates a new webhook config to receive HTTP notifications when specified events occur in the system."
    )]
    async fn webhook_create(
        &self,
        Parameters(args): Parameters<CreateWebhookParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.create_webhook_impl(args).await
    }

    #[tool(
        name = "webhook.get_webhook_by_event",
        description = "Retrieves a webhook configuration based on a specific event type, allowing users to find which webhook is set to trigger for that event."
    )]
    async fn webhook_get_webhook_by_event(
        &self,
        Parameters(args): Parameters<GetWebhookByEventWebhookParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.get_webhook_by_event_webhook_impl(args).await
    }

    // ===== ExperimentGroup =====
    #[tool(
        name = "experiment_group.create",
        description = "Creates a new experiment group."
    )]
    async fn experiment_group_create(
        &self,
        Parameters(args): Parameters<CreateExperimentGroupParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.create_experiment_group_impl(args).await
    }

    #[tool(
        name = "experiment_group.get",
        description = "Retrieves an existing experiment group by its ID."
    )]
    async fn experiment_group_get(
        &self,
        Parameters(args): Parameters<GetExperimentGroupParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.get_experiment_group_impl(args).await
    }

    #[tool(
        name = "experiment_group.update",
        description = "Updates an existing experiment group. Allows partial updates to specified fields."
    )]
    async fn experiment_group_update(
        &self,
        Parameters(args): Parameters<UpdateExperimentGroupParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.update_experiment_group_impl(args).await
    }

    #[tool(
        name = "experiment_group.delete",
        description = "Deletes an experiment group."
    )]
    async fn experiment_group_delete(
        &self,
        Parameters(args): Parameters<DeleteExperimentGroupParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.delete_experiment_group_impl(args).await
    }

    #[tool(
        name = "experiment_group.list",
        description = "Lists experiment groups, with support for filtering and pagination."
    )]
    async fn experiment_group_list(
        &self,
        Parameters(args): Parameters<ListExperimentGroupParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.list_experiment_group_impl(args).await
    }

    #[tool(
        name = "experiment_group.add_members_to_group",
        description = "Adds members to an existing experiment group."
    )]
    async fn experiment_group_add_members_to_group(
        &self,
        Parameters(args): Parameters<AddMembersToGroupExperimentGroupParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.add_members_to_group_experiment_group_impl(args).await
    }

    #[tool(
        name = "experiment_group.remove_members_from_group",
        description = "Removes members from an existing experiment group."
    )]
    async fn experiment_group_remove_members_from_group(
        &self,
        Parameters(args): Parameters<RemoveMembersFromGroupExperimentGroupParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.remove_members_from_group_experiment_group_impl(args).await
    }

    // ===== Variable =====
    #[tool(
        name = "variable.get",
        description = "Retrieves detailed information about a specific variable by its name."
    )]
    async fn variable_get(
        &self,
        Parameters(args): Parameters<GetVariableParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.get_variable_impl(args).await
    }

    #[tool(
        name = "variable.update",
        description = "Updates an existing variable's value, description, or tags."
    )]
    async fn variable_update(
        &self,
        Parameters(args): Parameters<UpdateVariableParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.update_variable_impl(args).await
    }

    #[tool(
        name = "variable.delete",
        description = "Permanently deletes a variable from the workspace."
    )]
    async fn variable_delete(
        &self,
        Parameters(args): Parameters<DeleteVariableParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.delete_variable_impl(args).await
    }

    #[tool(
        name = "variable.list",
        description = "Retrieves a paginated list of all variables in the workspace with optional filtering and sorting."
    )]
    async fn variable_list(
        &self,
        Parameters(args): Parameters<ListVariableParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.list_variable_impl(args).await
    }

    #[tool(
        name = "variable.create",
        description = "Creates a new variable with the specified name and value."
    )]
    async fn variable_create(
        &self,
        Parameters(args): Parameters<CreateVariableParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.create_variable_impl(args).await
    }

    // ===== Secret =====
    #[tool(
        name = "secret.get",
        description = "Retrieves detailed information about a specific secret by its name. The value is masked for security."
    )]
    async fn secret_get(
        &self,
        Parameters(args): Parameters<GetSecretParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.get_secret_impl(args).await
    }

    #[tool(
        name = "secret.update",
        description = "Updates an existing secret's value or description. The value is re-encrypted with the current workspace encryption key. Returns masked value."
    )]
    async fn secret_update(
        &self,
        Parameters(args): Parameters<UpdateSecretParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.update_secret_impl(args).await
    }

    #[tool(
        name = "secret.delete",
        description = "Permanently deletes a secret from the workspace. The encrypted value is removed and cannot be recovered."
    )]
    async fn secret_delete(
        &self,
        Parameters(args): Parameters<DeleteSecretParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.delete_secret_impl(args).await
    }

    #[tool(
        name = "secret.list",
        description = "Retrieves a paginated list of all secrets in the workspace with optional filtering and sorting. All secret values are masked."
    )]
    async fn secret_list(
        &self,
        Parameters(args): Parameters<ListSecretParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.list_secret_impl(args).await
    }

    #[tool(
        name = "secret.create",
        description = "Creates a new encrypted secret with the specified name and value. The secret is encrypted with the workspace's current encryption key. Secret values are never returned in responses for security."
    )]
    async fn secret_create(
        &self,
        Parameters(args): Parameters<CreateSecretParams>,
    ) -> Result<CallToolResult, rmcp::ErrorData> {
        self.create_secret_impl(args).await
    }

    // ===== MasterKey =====
    #[tool(
        name = "master_key.rotate_master_encryption_key",
        description = "Rotates the master encryption key across all workspaces"
    )]
    async fn master_key_rotate_master_encryption_key(&self) -> Result<CallToolResult, rmcp::ErrorData> {
        self.rotate_master_encryption_key_master_key_impl().await
    }

}
