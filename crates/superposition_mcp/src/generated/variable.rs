// AUTO-GENERATED by smithy mcp-codegen — DO NOT EDIT
use rmcp::model::*;
use schemars::JsonSchema;
use serde::Deserialize;

use crate::SuperpositionMcpServer;
use crate::helpers::*;

/// Retrieves detailed information about a specific variable by its name.
#[derive(Debug, Deserialize, JsonSchema)]
pub struct GetVariableParams {
    pub name: String,
}

/// Updates an existing variable's value, description, or tags.
#[derive(Debug, Deserialize, JsonSchema)]
pub struct UpdateVariableParams {
    pub name: String,
    pub value: Option<String>,
    pub description: Option<String>,
    pub change_reason: String,
}

/// Permanently deletes a variable from the workspace.
#[derive(Debug, Deserialize, JsonSchema)]
pub struct DeleteVariableParams {
    pub name: String,
}

/// Retrieves a paginated list of all variables in the workspace with optional filtering and sorting.
#[derive(Debug, Deserialize, JsonSchema)]
pub struct ListVariableParams {
    /// Number of items to be returned in each page.
    pub count: Option<i32>,
    /// Page number to retrieve, starting from 1.
    pub page: Option<i32>,
    /// If true, returns all requested items, ignoring pagination parameters page and count.
    pub all: Option<bool>,
    /// Filter by variable name (exact match or substring, depending on backend implementation).
    pub name: Option<Vec<String>>,
    /// Filter by the user who created the variable
    pub created_by: Option<Vec<String>>,
    /// Filter by the user who last modified the variable
    pub last_modified_by: Option<Vec<String>>,
    /// Field to sort the results by.
    pub sort_on: Option<String>,
    /// Sort order (ascending or descending).
    pub sort_by: Option<String>,
}

/// Creates a new variable with the specified name and value.
#[derive(Debug, Deserialize, JsonSchema)]
pub struct CreateVariableParams {
    pub name: String,
    pub value: String,
    pub description: String,
    pub change_reason: String,
}

impl SuperpositionMcpServer {
    pub async fn get_variable_impl(&self, args: GetVariableParams) -> Result<CallToolResult, rmcp::ErrorData> {
        let mut req = self.client.get_variable()
            .workspace_id(&self.config.workspace_id)
            .org_id(&self.config.org_id);
        req = req.name(args.name);
        let resp = req.send().await.map_err(mcp_err)?;
        let json = serde_json::to_string_pretty(&variable_to_json!(resp)).map_err(mcp_err)?;
        Ok(CallToolResult::success(vec![Content::text(json)]))
    }

    pub async fn update_variable_impl(&self, args: UpdateVariableParams) -> Result<CallToolResult, rmcp::ErrorData> {
        let mut req = self.client.update_variable()
            .workspace_id(&self.config.workspace_id)
            .org_id(&self.config.org_id);
        req = req.name(args.name);
        req = req.change_reason(args.change_reason);
        if let Some(v) = args.value {
            req = req.value(v);
        }
        if let Some(v) = args.description {
            req = req.description(v);
        }
        let resp = req.send().await.map_err(mcp_err)?;
        let json = serde_json::to_string_pretty(&variable_to_json!(resp)).map_err(mcp_err)?;
        Ok(CallToolResult::success(vec![Content::text(json)]))
    }

    pub async fn delete_variable_impl(&self, args: DeleteVariableParams) -> Result<CallToolResult, rmcp::ErrorData> {
        let mut req = self.client.delete_variable()
            .workspace_id(&self.config.workspace_id)
            .org_id(&self.config.org_id);
        req = req.name(args.name);
        let resp = req.send().await.map_err(mcp_err)?;
        let json = serde_json::to_string_pretty(&variable_to_json!(resp)).map_err(mcp_err)?;
        Ok(CallToolResult::success(vec![Content::text(json)]))
    }

    pub async fn list_variable_impl(&self, args: ListVariableParams) -> Result<CallToolResult, rmcp::ErrorData> {
        let mut req = self.client.list_variables()
            .workspace_id(&self.config.workspace_id)
            .org_id(&self.config.org_id);
        if let Some(v) = args.count {
            req = req.count(v);
        }
        if let Some(v) = args.page {
            req = req.page(v);
        }
        if let Some(v) = args.all {
            req = req.all(v);
        }
        if let Some(v) = args.name {
            for item in v {
                req = req.name(item);
            }
        }
        if let Some(v) = args.created_by {
            for item in v {
                req = req.created_by(item);
            }
        }
        if let Some(v) = args.last_modified_by {
            for item in v {
                req = req.last_modified_by(item);
            }
        }
        if let Some(v) = args.sort_on {
            req = req.sort_on(v);
        }
        if let Some(v) = args.sort_by {
            req = req.sort_by(v);
        }
        let resp = req.send().await.map_err(mcp_err)?;
        let items: Vec<serde_json::Value> = resp.data.iter().map(|r| variable_to_json!(r)).collect();
        let result = serde_json::json!({
            "total_pages": resp.total_pages,
            "total_items": resp.total_items,
            "data": items,
        });
        let json = serde_json::to_string_pretty(&result).map_err(mcp_err)?;
        Ok(CallToolResult::success(vec![Content::text(json)]))
    }

    pub async fn create_variable_impl(&self, args: CreateVariableParams) -> Result<CallToolResult, rmcp::ErrorData> {
        let mut req = self.client.create_variable()
            .workspace_id(&self.config.workspace_id)
            .org_id(&self.config.org_id);
        req = req.name(args.name);
        req = req.value(args.value);
        req = req.description(args.description);
        req = req.change_reason(args.change_reason);
        let resp = req.send().await.map_err(mcp_err)?;
        let json = serde_json::to_string_pretty(&variable_to_json!(resp)).map_err(mcp_err)?;
        Ok(CallToolResult::success(vec![Content::text(json)]))
    }

}
