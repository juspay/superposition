{
  "name": "context-aware-configuration",
  "version": "0.0.1",
  "private": true,
  "description": "This is just to run automated newman tests for this service",
  "scripts": {
    "test": "./node_modules/.bin/newmandir -n postman/cac"
  },
  "devDependencies": {
    "newmandir": "git+ssh://git@ssh.bitbucket.juspay.net/~natarajan_juspay.in/newmandir.git"
  }
}
