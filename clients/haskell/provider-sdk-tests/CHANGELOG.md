# Revision history for provider-sdk-tests

## Unreleased changes

* First version. Setup via the Haskell SDK (organisation, workspace,
  dimensions, default configs, overrides and experiments) followed by
  OpenFeature provider evaluation checks.
